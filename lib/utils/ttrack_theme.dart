
import 'package:flutter/material.dart';

btnStyle({required Color color}) => ElevatedButton.styleFrom(
      minimumSize: const Size(double.infinity, 50),
      elevation: 0,
      backgroundColor: const Color(0xFF194FE3),
      shape: const StadiumBorder(),
    );
